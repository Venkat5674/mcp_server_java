services:
  - type: web
    name: leetcode-github-mcp
    env: docker
    plan: free
    envVars:
      - key: GITHUB_TOKEN
        sync: false # Requires you to add this in the Render dashboard manually!
